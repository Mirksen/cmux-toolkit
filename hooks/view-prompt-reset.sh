#!/bin/bash
# UserPromptSubmit hook: no-op now that git is the source of truth.
# The browser view always reflects the current git working tree state.
# Kept as a hook entry point in case we want to add behavior later.

exit 0
